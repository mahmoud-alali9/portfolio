---
import DefaultLayout from '@/layouts/DefaultLayout.astro';
import ProjectsTabs from '@/components/sections/projects-tabs';
import { getAllProjects } from '@/lib/projects';

const projects = await getAllProjects();
---

<DefaultLayout title="Projets">
  <section class="hero-padding">
    <div class="container flex flex-col gap-5">
      <h1 class="text-3xl md:text-4xl">Projets & expérimentations</h1>
      <p class="text-muted-foreground text-lg leading-none">
        Une sélection de projets menés durant mes études, avec les
        métriques et le code qui vont avec
      </p>
    </div>

    <ProjectsTabs projects={projects} client:load />
  </section>
</DefaultLayout>
